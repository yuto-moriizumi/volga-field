{
  "name": "volga-field",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:server": "npm run dev --workspace apps/server",
    "build": "npm run build --workspace packages/shared && npm run build --workspace packages/game-core && npm run build --workspace apps/server && npm run build --workspace apps/web",
    "build:shared": "npm run build --workspace packages/shared",
    "build:core": "npm run build --workspace packages/game-core",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "husky": "^9.1.7",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
